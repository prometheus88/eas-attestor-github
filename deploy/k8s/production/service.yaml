apiVersion: v1
kind: Service
metadata:
  name: eas-validator
  namespace: production
  labels:
    app: eas-validator
    environment: production
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: eas-validator
    environment: production