apiVersion: v1
kind: Service
metadata:
  name: eas-validator
  namespace: staging
  labels:
    app: eas-validator
    environment: staging
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: eas-validator
    environment: staging