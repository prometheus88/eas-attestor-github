apiVersion: v1
kind: Service
metadata:
  name: attestor
  namespace: staging
  labels:
    app: attestor
    environment: staging
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: attestor
    environment: staging